// Copy this file to `lib/supabase_config.dart` (gitignored) and fill in the
// values from your Supabase project: Project Settings → API Keys.
//
// Use the "publishable" key ONLY — never the "secret" key in the app,
// it bypasses Row Level Security.
class SupabaseConfig {
  static const url = 'https://YOUR-PROJECT-REF.supabase.co';
  static const publishableKey = 'YOUR-PUBLISHABLE-KEY';
}
